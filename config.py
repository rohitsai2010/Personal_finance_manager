DB_CONFIG = {
    'host': 'localhost',
    'user': 'root',
    'password': 'Bunny@777',
    'database': 'finance_manager'
}
# we should never push sensitive information to github instead we can use .env or dotenv fo security. config.py should not be pushed into github
